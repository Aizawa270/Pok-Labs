# Pokémon RPG Bot (Stage 1 rebuild)

Staged bot bootstrap: Discord client, SQLite DB init, builder scripts to generate pokedex & route spawns.

Use GitHub Actions to run the builder and start the bot (workflow included).
